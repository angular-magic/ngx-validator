import { GetInterpolationDataPipe } from './get-interpolation-data.pipe';

describe('GetInterpolationDataPipe', () => {
  it('create an instance', () => {
    const pipe = new GetInterpolationDataPipe();
    expect(pipe).toBeTruthy();
  });
});
