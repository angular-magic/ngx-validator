import { GetErrorMessagePipe } from './get-error-message.pipe';

describe('GetErrorMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new GetErrorMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
