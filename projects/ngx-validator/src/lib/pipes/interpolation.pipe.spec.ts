import { InterpolationPipe } from './interpolation.pipe';

describe('InterpolationPipe', () => {
  it('create an instance', () => {
    const pipe = new InterpolationPipe();
    expect(pipe).toBeTruthy();
  });
});
